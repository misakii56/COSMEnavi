<div class="container">
<h2>アイシャドウ</h2>
  <% @eyeshadow.each do |e| %>
    <div>
      <%= link_to e.name, cosmetic_path(e.id)  %>
      <%= e.brand %>
      <%= e.color %>
    </div>
  <% end %>
</div>