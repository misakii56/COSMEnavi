<div class="container">
  <h4>ログイン</h4>
  <div class="row">
    <div class="col-12">
      <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>

        <div class="item" style="margin-top: 20px; font-size: 15px;"><%= f.label :email, "メールアドレス" %></div>
        <div class="item"><%= f.email_field :email, autofocus: true, autocomplete: "email" %></div>

        <div class="item" style="margin-top: 20px; font-size: 15px;"><%= f.label :password, "パスワード" %></div>
        <div class="item"><%= f.password_field :password, autocomplete: "current-password" %></div>

        <% if devise_mapping.rememberable? %>
          <div class="item"><%= f.check_box :remember_me %></div>
          <div class="item"><%= f.label :remember_me %></div>
        <% end %>

        <div class="item" style="margin-top: 20px;"><%= f.submit "ログイン", class:"btn btn-primary" %></div>
      <% end %>

      <div class="item" style="margin-top: 20px; font-size: 15px;">
        会員登録がお済みでない方はこちらから新規登録を行なってください。
      </div>
      <div class="item">
        <%= link_to new_user_registration_path, class: "btn btn--header btn--cubic btn--shadow" do %>新規登録
        <% end %>
      </div>
    </div>
  </div>
</div>